import { ErrorPageStrategy } from "shared/Result";

export const LotProduct = () => {
  return <div>LotProduct</div>;
};

export const Component = LotProduct;

export const ErrorBoundary = ErrorPageStrategy;
